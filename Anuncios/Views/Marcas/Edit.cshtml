@model Anuncios.Marca

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Marca</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IdMarca" />
            <div class="form-group">
                <label asp-for="NomeMarca" class="control-label">Nome da Marca</label>
                <input asp-for="NomeMarca" class="form-control" />
                <span asp-validation-for="NomeMarca" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Salvar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Voltar</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
